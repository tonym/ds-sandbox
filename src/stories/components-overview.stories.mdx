import { Meta } from '@storybook/addon-docs';
import { Components } from '../types/index';
import { isNumber } from '../helpers/index';

export const coreComponents = Object.keys(Components).filter(key => !isNumber(key));

<Meta title="Components/Overview" />

<h1>Overview - what are components?</h1>

<p>
  Components are the atoms of Gemini, pre-built presentational items with encapsulated style and rich data binding compatible with Angular.
</p>

<h2>Available components</h2>

<div>
  {coreComponents.map(component => (
    <p>{component}</p>
  ))}
</div>
